apiVersion: apps/v1
kind: Deployment
metadata:
  name: blog
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: blog
  template:
    metadata:
      labels:
        app: blog
    spec:
      containers:
        - name: blog
          image: ghcr.io/duchangkim/duchi-click@sha256:54f2b1072acecb01ea0618f6d3fb9f681a0ddb9e52d033491c6b114b26e82abc
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3011
          env:
            - name: NODE_ENV
              value: "production"
            - name: PORT
              value: "3011"
            - name: HOSTNAME
              value: "0.0.0.0"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: blog
  namespace: default
spec:
  ports:
    - port: 3011
      targetPort: 3011
  selector:
    app: blog
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: blog
  namespace: default
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  tls:
    - hosts:
        - duchi.click
      secretName: blog-tls
  rules:
    - host: duchi.click
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: blog
                port:
                  number: 3011
