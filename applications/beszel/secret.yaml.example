# 이 파일을 secret.yaml로 복사하고 실제 값을 입력하세요
# secret.yaml은 .gitignore에 의해 제외됩니다
apiVersion: v1
kind: Secret
metadata:
  name: beszel-secret
  namespace: default
type: Opaque
stringData:
  # Hub UI → Add System 에서 표시되는 Public Key
  ssh-public-key: "ssh-ed25519 AAAA... (Hub UI에서 복사)"
  # Hub UI → Settings → Tokens 에서 생성한 범용 토큰
  token: "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
