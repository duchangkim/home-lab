# 이 파일을 secret.yaml로 복사하고 실제 값을 입력하세요
# secret.yaml은 .gitignore에 의해 제외됩니다
apiVersion: v1
kind: Secret
metadata:
  name: beszel-secret
  namespace: default
type: Opaque
stringData:
  # Hub-Agent SSH 인증 키
  # 생성: ssh-keygen -t ed25519 -f beszel_key -N ""
  ssh-public-key: "ssh-ed25519 AAAA... (beszel_key.pub 내용)"
